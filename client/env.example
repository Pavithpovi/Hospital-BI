# Copy to .env.production locally, or set in Vercel → Project → Settings → Environment Variables.
# Must include the /api suffix (Flask routes are under /api).
#
# Example after you deploy the Python server (Render, Railway, Fly.io, etc.):
# VITE_API_URL=https://medvista-api.onrender.com/api

VITE_API_URL=
